"use strict";

exports.__esModule = true;
exports.BindEventMixin = BindEventMixin;

var _event = require("../utils/dom/event");

function BindEventMixin(handler) {
  function bind() {
    if (!this.binded) {
      handler.call(this, _event.on);
      this.binded = true;
    }
  }

  function unbind() {
    if (this.binded) {
      handler.call(this, _event.off);
      this.binded = false;
    }
  }

  return {
    mounted: bind,
    activated: bind,
    destroyed: unbind,
    deactivated: unbind
  };
}